#include <iostream>
#include <string>

int main()
{
	std::string MyLukic = "MeinLiebe";
	MyLukic.resize(MyLukic.size()+1);

	for (int slovo = MyLukic.size()-1; slovo >= 3; slovo--)
	{
		MyLukic[slovo + 1] = MyLukic[slovo];
	}
	MyLukic[4] = 'e';
	std::cout << MyLukic;
}

